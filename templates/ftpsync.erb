#!/bin/sh

set -e

FTPSYNC=/usr/bin/ftpsync

[ -x ${FTPSYNC} ] || exit 0

cd <%= @homedir %>
<% if @debian_push_user != '' -%>
su <%= @debian_push_user -%> -s /bin/sh -c 'ftpsync'
<% else -%>
su ftp -s /bin/sh -c 'ftpsync'
<% end -%>
